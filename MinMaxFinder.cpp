#include <iostream>

using namespace std;

int main() {
    int numbers[] = {23, 5, 67, -2, 45, 87, 1, 99, 34, -10};
    int min = numbers[0];
    int max = numbers[0];

    for(int i = 1; i < size_of(numbers); i++) {
        if(numbers[i] > max) {
            max
        }
    }
}